# Micro Donation Platform - Visual Architecture Guide

## 🏗️ Application Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     React Native (Expo)                     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                 App.js (Main Component)              │  │
│  │                                                      │  │
│  │  • All state management (useState hooks)             │  │
│  │  • All screens (render functions)                    │  │
│  │  • All business logic (functions)                    │  │
│  │  • All styling (StyleSheet)                          │  │
│  │  • All modals and dialogs                            │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                    Data Store (State)                │  │
│  │                                                      │  │
│  │  📦 users[]           • Authentication              │  │
│  │  📦 causes[]          • Causes management            │  │
│  │  📦 donations[]       • Donation history             │  │
│  │  📦 causeRequests[]   • Join requests                │  │
│  │  📦 currentUser       • Session info                 │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Navigation (currentScreen)              │  │
│  │                                                      │  │
│  │  home                                                │  │
│  │  ├─ register                                         │  │
│  │  ├─ login                                            │  │
│  │  ├─ whereToDonate                                   │  │
│  │  ├─ userDashboard                                    │  │
│  │  ├─ causeDetail                                      │  │
│  │  └─ adminDashboard                                   │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │               Modals (Always Available)              │  │
│  │                                                      │  │
│  │  • Donation Modal                                    │  │
│  │  • Payment Instructions Modal                        │  │
│  │  • Add Cause Modal (Admin)                           │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
    React Native API
         │
    ┌────┴────┬───────────┬──────────┐
    ▼         ▼           ▼          ▼
  iOS       Android     Web      Expo Go
```

---

## 🎯 Data Flow Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    User Interaction                     │
│  (Button press, text input, modal open, etc.)         │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────┐
        │   Event Handler Function    │
        │  (handleDonate, handleLogin, │
        │   handleAddCause, etc.)      │
        └──────────────────┬───────────┘
                           │
                  ┌────────┴────────┐
                  │                 │
                  ▼                 ▼
         ┌─────────────────┐  ┌──────────────┐
         │  Validation &   │  │   State      │
         │  Business Logic │  │   Update     │
         └────────┬────────┘  └────────┬─────┘
                  │                    │
                  └────────┬───────────┘
                           │
                           ▼
            ┌──────────────────────────────┐
            │  Update Component State      │
            │  (useState setters)          │
            └─────────────┬────────────────┘
                          │
                          ▼
            ┌──────────────────────────────┐
            │  Component Re-renders        │
            │  with new state              │
            └─────────────┬────────────────┘
                          │
                          ▼
            ┌──────────────────────────────┐
            │  Display Updated UI          │
            │  to User                     │
            └──────────────────────────────┘
```

---

## 📱 Screen Navigation Map

```
                          ┌─────────┐
                          │  Home   │
                          └────┬────┘
                    ┌─────────┬┼──┬──────────────┐
                    │         │   │              │
                    ▼         ▼   ▼              ▼
            ┌──────────────┐ │   │     ┌──────────────────┐
            │  Register    │ │   │     │ Where to Donate  │
            │  (User)      │ │   │     └──────────────────┘
            └──────┬───────┘ │   │
                   │         │   │
            ┌──────────────┐ │   │
            │  Register    │ │   │
            │  (Admin)     │ │   │
            └──────┬───────┘ │   │
                   │         │   │
                   │   ┌─────▼──▼─────┐
                   │   │    Login     │
                   │   └──────┬───────┘
                   │          │
          ┌────────▼─ ───┬────▼────────────────┐
          │               │                    │
          ▼               ▼                    ▼
    ┌──────────────┐ ┌──────────────┐  ┌─────────────────┐
    │ User         │ │ Admin        │  │ Where to Donate │
    │ Dashboard    │ │ Dashboard    │  │ (after login)   │
    └──┬───────┬───┘ └──┬─────┬──────┘  └─────────────────┘
       │       │        │     │
       │       │        │     └─────────────┐
       │       │        │                   │
       │       │        ├─────────────────┐ │
       │       │        │                 │ │
       │       │        ▼                 ▼ ▼
       │       │   ┌──────────────┐  ┌─────────────┐
       │       │   │ Add New Cause│  │ Pending     │
       │       │   │ (Modal)      │  │ Requests    │
       │       │   └──────────────┘  └─────────────┘
       │       │
       │       └───────────────┐
       │                       │
       ▼                       ▼
   ┌─────────────────┐  ┌──────────────────┐
   │ Cause Detail    │  │ Donate Modal     │
   │ Page            │  │ - Amount input   │
   └──┬──────────┬───┘  │ - Payment method │
      │          │      │ - Confirm button │
      │          │      └────────┬─────────┘
      │          │               │
      │          │      ┌────────▼─────────┐
      │          │      │ Payment          │
      │          │      │ Instructions     │
      │          │      │ (GCash/Bank)     │
      │          │      └──────────────────┘
      │          │
      ▼          ▼
   ┌──────────────────────────────┐
   │ Request to Join Modal        │
   └──────────────────────────────┘
```

---

## 🔄 State Management Flow

### User Authentication Flow
```
┌──────────────┐
│ Home Screen  │
└──────┬───────┘
       │ User enters credentials
       ▼
┌──────────────────────────┐
│ Login Form               │
│ - email input            │
│ - password input         │
│ - "Login" button         │
└──────┬───────────────────┘
       │ Submit
       ▼
┌──────────────────────────┐
│ Validation               │
│ - Fields not empty?      │
│ - Email exists?          │
│ - Password matches?      │
└──────┬───────────────────┘
       │ Valid
       ▼
┌──────────────────────────┐
│ Update State             │
│ - setCurrentUser(user)   │
│ - setIsLoggedIn(true)    │
│ - setCurrentScreen(      │
│   'userDashboard' or     │
│   'adminDashboard')      │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Dashboard Rendered       │
│ with user data           │
└──────────────────────────┘
```

### Donation Flow
```
┌─────────────────┐
│ Cause Card      │
│ "Donate" button │
└────────┬────────┘
         │ Click
         ▼
┌──────────────────────────┐
│ setShowDonationModal     │
│ setSelectedCause(cause)  │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Donation Modal Opens     │
│ - Amount input           │
│ - Payment method select  │
│ - Confirm button         │
└────────┬─────────────────┘
         │ User enters amount & selects payment
         ▼
┌──────────────────────────┐
│ handleDonate()           │
│ - Validate amount        │
│ - Check balance (if app) │
│ - Create donation record │
│ - Update cause.raised    │
│ - Update user.balance    │
│ - Close modal            │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ "Success" Alert          │
│ Donation recorded        │
└──────────────────────────┘
```

---

## 🎨 Component Hierarchy

```
SafeAreaView (root)
├── Header
│   ├── Title (Text)
│   ├── Subtitle (Text)
│   └── Balance Display (if user logged in)
│
├── ScrollView (content)
│   ├── Causes List (map)
│   │   └── CauseCard (View)
│   │       ├── Title (TouchableOpacity)
│   │       ├── Category (Text)
│   │       ├── Progress Bar (View)
│   │       ├── Progress % (Text)
│   │       ├── Raised/Goal (Text)
│   │       └── Donate Button (TouchableOpacity)
│   │
│   ├── Donations List (map)
│   │   └── DonationCard (View)
│   │       ├── Cause Name (Text)
│   │       ├── Amount (Text)
│   │       └── Date (Text)
│   │
│   └── Buttons
│       ├── Logout Button
│       └── Back Button
│
├── Modal: Donation
│   ├── Title (Text)
│   ├── Balance (Text)
│   ├── Amount Input (TextInput)
│   ├── Quick Amounts (TouchableOpacity x4)
│   ├── Payment Method Selector
│   │   ├── In-App (TouchableOpacity)
│   │   ├── GCash (TouchableOpacity)
│   │   └── Online (TouchableOpacity)
│   ├── Confirm Button (TouchableOpacity)
│   └── Cancel Button (TouchableOpacity)
│
├── Modal: Payment Instructions
│   ├── Title (Text)
│   ├── Payment Details (Text)
│   ├── I have paid Button (TouchableOpacity)
│   └── Close Button (TouchableOpacity)
│
└── Modal: Add Cause (Admin)
    ├── Title (Text)
    ├── Title Input (TextInput)
    ├── Description Input (TextInput)
    ├── Goal Input (TextInput)
    ├── Image URL Input (TextInput)
    ├── Create Button (TouchableOpacity)
    └── Cancel Button (TouchableOpacity)
```

---

## 🔀 Function Call Flow

### Complete Donation Flow

```
User clicks "Donate" button
    ↓
openDonationModal(cause)
    ↓
setSelectedCause(cause)
setPaymentMethod('balance')
setShowDonationModal(true)
    ↓
[User sees Donation Modal]
[User enters amount and selects payment]
    ↓
User clicks "Confirm Donation"
    ↓
if (paymentMethod === 'balance') {
    handleDonate()
} else {
    setShowPaymentInstructions(true)
}
    ↓
Donation Modal closes
    ↓
[User sees Payment Instructions Modal for GCash/Bank]
    ↓
User clicks "I have paid"
    ↓
setShowPaymentInstructions(false)
handleDonate()
    ↓
handleDonate() {
    ✓ Validate amount
    ✓ Check balance (if in-app)
    ✓ Create donation object
    ✓ setDonations([...donations, newDonation])
    ✓ setCauses(causes.map(...update raised amount...))
    ✓ if (paymentMethod === 'balance') {
        setCurrentUser({...update balance...})
      }
    ✓ setShowDonationModal(false)
    ✓ setDonationAmount('')
    ✓ Alert.alert('Success', '...')
}
    ↓
Success Alert shown
Donation recorded in history
Dashboard re-renders with updated data
```

---

## 📊 State Tree

```
App State
├── Authentication
│   ├── isLoggedIn: boolean
│   ├── currentUser: User | null
│   ├── userType: 'user' | 'admin' | ''
│   ├── email: string
│   ├── password: string
│   ├── name: string
│   ├── confirmPassword: string
│   └── users: User[]
│
├── Navigation
│   └── currentScreen: 'home' | 'login' | 'register' | 'userDashboard' | ...
│
├── Causes & Donations
│   ├── causes: Cause[]
│   ├── selectedCause: Cause | null
│   ├── donations: Donation[]
│   └── causeRequests: CauseRequest[]
│
├── UI State - Donation
│   ├── showDonationModal: boolean
│   ├── donationAmount: string
│   ├── paymentMethod: 'balance' | 'gcash' | 'online'
│   └── showPaymentInstructions: boolean
│
├── UI State - Admin
│   ├── showAddCauseModal: boolean
│   ├── newCauseTitle: string
│   ├── newCauseDescription: string
│   ├── newCauseGoal: string
│   ├── newCauseCategory: string
│   └── newCauseImage: string
│
└── Data
    ├── users: [User]
    ├── causes: [Cause]
    ├── donations: [Donation]
    └── causeRequests: [CauseRequest]
```

---

## 🔌 Integration Points

### For Future Backend Integration

```
                    ┌─────────────────┐
                    │  Backend Server │
                    │  (Node.js/etc)  │
                    └────────┬────────┘
                             │
           ┌─────────────────┼─────────────────┐
           │                 │                 │
           ▼                 ▼                 ▼
    ┌────────────────┐ ┌──────────┐  ┌──────────────┐
    │ Authentication │ │ Causes   │  │ Payments API │
    │ /login         │ │ /causes  │  │ /pay         │
    │ /register      │ │ /create  │  │ /webhook     │
    │ /verify        │ │ /update  │  │ /verify      │
    └────────────────┘ └──────────┘  └──────────────┘
           │                 │                 │
           └─────────────────┼─────────────────┘
                             │
                    ┌────────▼────────┐
                    │  Database       │
                    │  (MongoDB/SQL)  │
                    └─────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Expo (Cloud Build)                    │
│                                                         │
│  npm run build:android                                  │
│       ↓                                                 │
│  ┌────────────────────────────┐                        │
│  │ Android APK/AAB            │                        │
│  │ Published to Google Play    │                        │
│  └────────────────────────────┘                        │
│                                                         │
│  npm run build:ios                                      │
│       ↓                                                 │
│  ┌────────────────────────────┐                        │
│  │ iOS IPA                    │                        │
│  │ Published to App Store     │                        │
│  └────────────────────────────┘                        │
│                                                         │
│  npm run build:web                                      │
│       ↓                                                 │
│  ┌────────────────────────────┐                        │
│  │ Web Version                │                        │
│  │ Deploy to Netlify/Vercel   │                        │
│  └────────────────────────────┘                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📈 Performance Considerations

```
┌─────────────────────────────────────┐
│         Code Optimization           │
├─────────────────────────────────────┤
│ Current                             │
│ • Simple state management           │
│ • Functional components             │
│ • No external libraries (minimal)   │
│                                     │
│ Optimization Opportunities          │
│ ✓ useCallback for handlers          │
│ ✓ useMemo for calculations          │
│ ✓ useReducer for complex state      │
│ ✓ React.memo for components         │
│ ✓ FlatList for large lists          │
│ ✓ Image lazy loading                │
│ ✓ Code splitting                    │
└─────────────────────────────────────┘
```

---

## 🎯 Feature Roadmap

```
Version 1.0 (CURRENT) ✅
├─ User authentication
├─ Admin cause management
├─ Multi-payment support
├─ Request workflows
└─ Donation tracking

Version 1.1 (READY) 🔜
├─ AsyncStorage persistence
├─ Image upload (expo-image-picker)
├─ Push notifications
└─ Search & filter

Version 2.0 (PLANNED) 🗓️
├─ Backend API integration
├─ Real payment processing
├─ Social sharing
├─ User profiles
└─ Advanced analytics
```

---

This visual guide helps understand the complete architecture and how all components work together! 
